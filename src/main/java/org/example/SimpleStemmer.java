package org.example;

/**
 * Was previously trying to convert code taken from: https://github.com/words/stemmer/blob/main/index.js
 * into Java but abandoned as it was a bit of a headache.
 */

public class SimpleStemmer {
    static String stem(String word) {
        return word.toLowerCase();
    }
}
